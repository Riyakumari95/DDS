#include<stdio.h>
#include<stdlib.h>
int main(){
    int n;
    printf("enter the size of array:");
    scanf("%d",&n);
    
    int * arr = (int*)malloc(n*sizeof(int));
    
    for(int i=0;i<n;i++){
        printf("\n enter the element of arr[%d] index:",i);
        scanf("%d",&arr[i]);
        }
        printf("\n entered elements in array:");
        for(int i=0;i<n;i++){
            printf("%d",arr[i]);
        }
        return 0;
}


output:
enter the size of array:5
 enter the element of arr[0] index:9
 enter the element of arr[1] index:3
 enter the element of arr[2] index:0
 enter the element of arr[3] index:4
 enter the element of arr[4] index:7
 entered elements in array:93047
