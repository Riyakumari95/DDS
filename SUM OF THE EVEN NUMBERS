#include<stdio.h>
int main(){
    int sum=0; //Initialize sum to 0
    int i = 0; //Declare loop counter variable 
    for(i=0;i<=20;i++){
        if(i%2==0){
            (sum+=i); //Compound assignment operator 
        }
    }
    printf("Sum of all even numbers from 1 to 20 is : %d\n",sum);
    return 0;
}


OUTPUT:
Sum of all even numbers from 1 to 20 is : 110
